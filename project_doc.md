# C++ Logic & Loop Counter - Проектная документация

## 1. Техническое задание

### Цель проекта
Разработать расширение для VS Code, которое автоматически анализирует и визуализирует статистику логических конструкций и циклов в C++ исходном коде, предоставляя разработчикам мгновенную информацию о сложности и структуре их программ.

### Основной функционал
- Автоматический подсчет логических конструкций (if, else if, else) и циклов (for, while, do-while)
- Отображение статистики в строке состояния VS Code в реальном времени
- Детальный анализ по требованию через всплывающие уведомления
- Реакция на изменения кода без задержек
- Поддержка файлов C и C++

### Измеряемые метрики
1. **Условные операторы:**
   - Количество `if` операторов
   - Количество `else if` операторов  
   - Количество `else` операторов
2. **Циклы:**
   - Количество `for` циклов
   - Количество `while` циклов
   - Количество `do-while` циклов
3. **Агрегированные показатели:**
   - Общее количество логических конструкций

### Технические требования
- **Язык разработки**: TypeScript
- **Платформа**: VS Code Extension API
- **Совместимость**: VS Code 1.60.0+
- **Производительность**: Обработка файлов до 10,000 строк
- **Точность**: Корректное различение `else if` от отдельных `else` и `if`

### Пользовательский интерфейс
- **Строка состояния**: Компактный формат с иконками VS Code
- **Подсказка при наведении**: Детальная статистика с форматированием
- **Команды**: Ручное обновление и детальный просмотр
- **Уведомления**: Информационные сообщения о действиях

### Ограничения
- Поддержка только языков C и C++
- Анализ на основе регулярных выражений (синтаксический анализ ограничен)
- Не учитываются макросы препроцессора
- Только английские ключевые слова

# C++ Logic & Loop Counter - Архитектура и реализация

## Блок-схема работы расширения

```
[Активация расширения]
          ↓
[Создание элемента строки состояния]
          ↓
[Инициализация анализатора конструкций]
          ↓
[Подписка на события VS Code:        ]
[  • Изменение текста                ]
[  • Переключение редактора          ]
[  • Сохранение файла                ]
          ↓
[Запуск первичного анализа]
          ↓
[Цикл обработки событий:             ]
[  • Получение текста из редактора   ]
[  • Парсинг регулярными выражениями ]
[  • Подсчет конструкций             ]
[  • Обновление строки состояния     ]
          ↓
[Деактивация с очисткой ресурсов]
```

## 2. Детали реализации

### Алгоритм подсчета конструкций

#### Шаг 1: Парсинг регулярными выражениями
```typescript
// Алгоритм подсчета для каждого типа конструкции:
1. if: /\bif\s*\(/g
   - Ищет слово "if" за которым следует открывающая скобка
   - Учитывает пробелы между "if" и "("
   
2. else if: /\belse\s+if\s*\(/g  
   - Ищет "else if" с минимум одним пробелом между ними
   - Исключает ложные срабатывания для раздельных else и if
   
3. while: /\bwhile\s*\(/g
   - Подсчитывает циклы while
   - Требуется для корректного исключения do-while
   
4. do-while: /\bdo\b/g
   - Подсчитывает ключевые слова "do"
   - Каждое "do" считается началом do-while цикла
   
5. for: /\bfor\s*\(/g
   - Ищет циклы for в любом формате
   
6. else: /\belse\b/g
   - Общее количество else
   - Затем вычитается количество else if
```

#### Шаг 2: Расчет статистики
```typescript
// Математическая модель подсчета:
totalElse = matches(/\belse\b/g)           // Все вхождения else
elseIfCount = matches(/\belse\s+if\s*\(/g) // Специфические else if
pureElse = totalElse - elseIfCount         // Else без последующего if

total = if + elseIf + pureElse + while + doWhile + for
```

### Математическая модель

#### Формулы подсчета
```
if_count = count_matches(/\bif\s*\(/g)
else_if_count = count_matches(/\belse\s+if\s*\(/g)  
else_count = count_matches(/\belse\b/g) - else_if_count
while_count = count_matches(/\bwhile\s*\(/g)
do_while_count = count_matches(/\bdo\b/g)
for_count = count_matches(/\bfor\s*\(/g)

total_complexity = Σ(if_count, else_if_count, else_count, 
                     while_count, do_while_count, for_count)
```

#### Пример работы анализатора:
```
Исходный код C++:
if (x > 0) {                    // if_count = 1
    for (int i = 0; i < n; i++) {  // for_count = 1
        // тело цикла
    }
} else if (x < 0) {            // else_if_count = 1
    while (condition) {        // while_count = 1
        do {                   // do_while_count = 1
            // тело
        } while (test);
    }
} else {                       // else_count = 1
    // альтернатива
}

Результат анализа:
if: 1, else if: 1, else: 1, while: 1, do-while: 1, for: 1, total: 6
```

### Форматы отображения

#### Вариант 1: Иконный формат (используется по умолчанию)
```
$(symbol-boolean)5 $(symbol-loop)3 $(refresh)2
где:
  $(symbol-boolean) - иконка if операторов (количество: 5)
  $(symbol-loop)    - иконка for циклов (количество: 3)  
  $(refresh)        - иконка while циклов (количество: 2)
```

#### Вариант 2: Компактный формат
```
C++: 15 (if:5 for:4)
```

#### Вариант 3: Детальный формат
```
if:5 eif:3 e:2 for:4 wh:3 dw:1
```

### Структура подсказки (tooltip)
```
C++ Logic Constructs:
─────────────────
If: 5
Else If: 3
Else: 2
For: 4
While: 3
Do-While: 1
─────────────────
Total: 18
```

### Формат детального уведомления
```
📊 C++ Code Analysis
══════════════════
Conditional Statements:
• If: 5
• Else If: 3  
• Else: 2

Loop Statements:
• For: 4
• While: 3
• Do-While: 1

══════════════════
Total Logical Constructs: 18
```

## 3. Система событий и реактивность

### Архитектура событийной модели
```
     [Пользовательские действия]
            ↓
[Системные события VS Code] → [Обработчики расширения]
            ↓
     [Обновление интерфейса]
```

### Зарегистрированные обработчики событий:

#### 1. Изменение текста (`onDidChangeTextDocument`)
```typescript
// Триггер: Любое изменение в документе
// Действие: Немедленное обновление статистики
// Особенность: Проверка активного редактора для оптимизации
```

#### 2. Переключение редактора (`onDidChangeActiveTextEditor`)
```typescript
// Триггер: Смена активной вкладки
// Действие: Анализ нового файла
// Особенность: Скрытие строки состояния для не-C++ файлов
```

#### 3. Сохранение файла (`onDidSaveTextDocument`)
```typescript
// Триггер: Сохранение файла (Ctrl+S)
// Действие: Финализация статистики
// Особенность: Гарантия актуальных данных при сохранении
```

### Машина состояний расширения
```
Состояния:
1. ИНИЦИАЛИЗАЦИЯ → создание ресурсов
2. АКТИВНЫЙ C++ → анализ и отображение
3. НЕАКТИВНЫЙ   → скрытие строки состояния
4. ДЕАКТИВАЦИЯ → очистка ресурсов

Переходы:
- Открытие C++ файла → АКТИВНЫЙ C++
- Закрытие файла → ИНИЦИАЛИЗАЦИЯ
- Переход к не-C++ файлу → НЕАКТИВНЫЙ
- Выгрузка расширения → ДЕАКТИВАЦИЯ
```

## 4. Производительность и оптимизация

### Метрики производительности

#### Временные характеристики:
- **Парсинг 100 строк**: < 5 мс
- **Обновление строки состояния**: < 2 мс  
- **Полный цикл обработки события**: < 10 мс
- **Инициализация расширения**: < 50 мс

#### Ресурсные требования:
- **Память (статика)**: ~50 KB
- **Память (динамика)**: ~10 KB на файл
- **CPU при простое**: 0%
- **CPU при активном редактировании**: < 1%

### Оптимизации реализации

#### 1. Ленивая инициализация
```typescript
// Ресурсы создаются только при необходимости
// Строка состояния скрыта для не-C++ файлов
// Анализ выполняется только для видимых редакторов
```

#### 2. Кэширование результатов
```typescript
// При частых изменениях в одной строке
// Результаты могут кэшироваться на короткое время
// Автоматическая инвалидация при значительных изменениях
```

#### 3. Батчинг событий
```typescript
// Быстрые последовательные изменения
// Группируются в одно обновление
// Предотвращение "мерцания" интерфейса
```

#### 4. Эффективное использование регулярных выражений
```typescript
// Предкомпилированные шаблоны
// Минимизация backtracking
// Точечное применение к нужным конструкциям
```

### Граничные случаи и обработка ошибок

#### Поддерживаемые сценарии:
- **Пустые файлы** - возврат нулевых статистик
- **Файлы без логических конструкций** - корректное отображение нулей
- **Очень большие файлы** (10k+ строк) - постепенная обработка
- **Смешанный код C/C++** - корректная идентификация языка
- **Вложенные комментарии** - игнорирование ключевых слов в комментариях

#### Обработка ошибок парсинга:
1. **Непарные скобки в паттернах** → fallback к упрощенному анализу
2. **Некорректные регулярные выражения** → использование запасных паттернов
3. **Исключения при анализе** → сохранение предыдущих валидных результатов
4. **Проблемы с обновлением UI** → повторная попытка через 100 мс

#### Специфические случаи C++:
- **Шаблоны и макросы**: Избегание ложных срабатываний
- **Строковые литералы**: Игнорирование ключевых слов в кавычках
- **Многострочные комментарии**: Пропуск всего содержимого комментариев
- **Препроцессорные директивы**: Ограниченная поддержка (#if, #ifdef)

## 5. Пользовательские сценарии

### Сценарий 1: Ежедневная разработка
```
1. Разработчик открывает файл main.cpp
2. Строка состояния автоматически показывает: $(symbol-boolean)3 $(symbol-loop)2 $(refresh)1
3. При наведении мыши видит детальную статистику
4. Во время редактирования цифры обновляются в реальном времени
5. При сохранении (Ctrl+S) статистика финализируется
```

### Сценарий 2: Анализ существующего кода
```
1. Разработчик открывает legacy-проект
2. Видит высокие показатели в строке состояния
3. Вызывает команду "C++ Counter: Show Details"
4. Получает полную картину сложности кода
5. Принимает решение о рефакторинге
```

### Сценарий 3: Обучение и код-ревью
```
1. Преподаватель проверяет студенческие работы
2. Быстрая оценка сложности алгоритмов по цифрам
3. Выявление чрезмерно сложных функций
4. Объективные метрики для обратной связи
```

### Визуальные примеры интерфейса:

#### Нормальный файл:
```
[VS Code Status Bar] ... $(symbol-boolean)2 $(symbol-loop)1 $(refresh)0
```

#### Сложный файл (предупреждение):
```
[VS Code Status Bar] ... $(symbol-boolean)12 $(symbol-loop)8 $(refresh)5
```

#### Подсказка при наведении:
```
┌─────────────────────────────────────┐
│ C++ Logic Constructs:               │
│ ─────────────────                   │
│ If: 12                              │
│ Else If: 5                          │
│ Else: 3                             │
│ For: 8                              │
│ While: 5                            │
│ Do-While: 2                         │
│ ─────────────────                   │
│ Total: 35                           │
└─────────────────────────────────────┘
```

## 6. Расширяемость и будущие улучшения

### Планируемые функции:
1. **Конфигурируемые пороги** - предупреждения при превышении сложности
2. **Исторический анализ** - отслеживание изменений сложности во времени
3. **Визуализация распределения** - графики в сайдбаре VS Code
4. **Поддержка других языков** - C#, Java, Python
5. **Интеграция с метриками качества** - связь с cyclomatic complexity

### Архитектурные возможности для расширения:
- **Модульная система анализаторов** - плагины для разных языков
- **Абстрактная фабрика статистик** - единый интерфейс для всех метрик
- **Конвейер обработки событий** - pipeline для сложных анализов
- **Внешнее API** - возможность интеграции с CI/CD системами

### Технический долг и ограничения:
1. **Текущие ограничения**:
   - Только регулярные выражения (не full parser)
   - Нет учета вложенности конструкций
   - Ограниченная обработка макросов препроцессора

2. **Пути улучшения**:
   - Интеграция с Clang для семантического анализа
   - Кэширование AST для больших проектов
   - Инкрементальный парсинг при изменениях