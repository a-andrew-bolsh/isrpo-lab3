# C++ Logic & Loop Counter - Расширение для VS Code

## Обзор
Умный анализатор кода для C++ разработчиков, который автоматически подсчитывает логические конструкции и циклы в реальном времени. Отображает статистику прямо в строке состояния VS Code, помогая оценить сложность и структуру кода.

## Особенности

### Автоматический анализ кода
- **Реальное время**: Статистика обновляется при каждом изменении
- **Все основные конструкции**: if/else if/else, for/while/do-while  
- **Точное различение**: Правильно определяет else if и отдельные else
- **Поддержка C/C++**: Работает с файлами .cpp, .c, .h, .hpp

### Визуализация в интерфейсе
- **Строка состояния**: Компактный формат с иконками VS Code
- **Подсказки при наведении**: Детальная статистика с красивым форматированием
- **Команды быстрого доступа**: Обновление и детальный просмотр по требованию
- **Автоматическая настройка**: Работает сразу после установки

## Как это выглядит

### В строке состояния VS Code
```
[VS Code Status Bar] ... $(symbol-boolean)3 $(symbol-loop)2 $(refresh)1
```

**Расшифровка:**
- `$(symbol-boolean)` - количество if операторов (3)
- `$(symbol-loop)` - количество for циклов (2)  
- `$(refresh)` - количество while циклов (1)

### Подсказка при наведении
```
C++ Logic Constructs:
─────────────────
If: 3
Else If: 1
Else: 2
For: 2
While: 1
Do-While: 0
─────────────────
Total: 9
```

### Детальный анализ
```
C++ Code Analysis
══════════════════
Conditional Statements:
• If: 3
• Else If: 1  
• Else: 2

Loop Statements:
• For: 2
• While: 1
• Do-While: 0

══════════════════
Total Logical Constructs: 9
```

## Установка

### Из VS Code Marketplace
1. Откройте VS Code
2. Нажмите Ctrl+Shift+X (Extensions)
3. Найдите "C++ Logic & Loop Counter"
4. Нажмите Install

### Из VSIX файла
```bash
code --install-extension cpp-logic-counter-1.0.0.vsix
```

## Использование

### Автоматический режим
1. **Откройте C/C++ файл** - статистика появится автоматически
2. **Редактируйте код** - цифры обновляются в реальном времени
3. **Наведите курсор** на цифры в строке состояния - увидите детали

### Ручное управление

#### Обновить статистику
```
Команда: C++ Counter: Update Statistics
Действие: Принудительное обновление счетчиков
Уведомление: "C++ counter updated"
```

#### Показать детали
```
Команда: C++ Counter: Show Details  
Действие: Отображение полного анализа во всплывающем окне
```

## Примеры кода с анализом

### Простой случай
```cpp
// Статистика: if:1, for:1, total:2

int main() {
    int x = 10;
    
    if (x > 5) {                    // if: 1
        for (int i = 0; i < 3; i++) { // for: 1
            // код
        }
    }
    
    return 0;
}
```

### Сложный случай
```cpp
// Статистика: if:2, else if:1, else:1, for:1, while:1, do-while:1, total:7

void processData(int* data, int size) {
    if (data == nullptr) {           // if: 1
        return;
    }
    
    int i = 0;
    while (i < size) {               // while: 1
        if (data[i] < 0) {           // if: 2 (вложенный)
            do {                     // do-while: 1
                data[i] = 0;
            } while (validate(data[i]));
        } else if (data[i] > 100) {  // else if: 1
            data[i] = 100;
        } else {                     // else: 1
            // ничего не делаем
        }
        i++;
    }
    
    for (int j = 0; j < size; j++) { //  for: 1
        // обработка
    }
}
```

## Преимущества

### Для разработчиков
- **Быстрая оценка сложности**: Мгновенное понимание структуры кода
- **Выявление проблем**: Обнаружение слишком сложных функций
- **Объективные метрики**: Количественные данные для рефакторинга
- **Фокус на качестве**: Постоянная обратная связь о сложности

### Для команд
- **Единые стандарты**: Объективная оценка сложности кода
- **Упрощенный код-ревью**: Быстрая проверка сложности функций
- **Обучение новичков**: Наглядные метрики для обучения

## Поддерживаемые метрики

### Условные операторы
```
if (condition)         // if
else if (condition)    // else if  
else                   // else
```

### Циклы
```
for (init; condition; increment)  // for
while (condition)                 // while
do { } while (condition)          // do-while
```

### Агрегированные показатели
```
Общее количество = if + else if + else + for + while + do-while
```

## 🔧 Технические детали

### Поддерживаемые языки
- **C++** (.cpp, .cc, .cxx, .hpp, .h)
- **C** (.c, .h)

### Требования
- **VS Code**: версия 1.60.0 или выше
- **Память**: 50MB свободной памяти

## Ограничения

### Текущие ограничения
- Только C/C++ языки
- Анализ на основе регулярных выражений
- Не учитываются макросы препроцессора
- Только английские ключевые слова

